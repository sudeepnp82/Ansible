---
 - hosts: redhat
   tasks:
   - name: install httpd
     yum:
       name: httpd
       state: present
   - name: start the service
     service:
       name: httpd
       state: started
   - name: deployment
     copy:
        src: index.html
        dest: /var/www/html
...
